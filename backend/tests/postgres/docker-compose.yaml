version: '3.2'
services:
  postgres:
    image: postgres:17
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=admin
      - POSTGRES_DB=test_db
    ports:
      - '5432:5432'
    volumes:
      - ~/test_pg_data:/var/lib/postgresql/data
