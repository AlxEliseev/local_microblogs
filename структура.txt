twitter-clone/
├── backend/              # FastAPI API
│   ├── app/              # Основное приложение
│   │   ├── __init__.py
│   │   ├── main.py       # app = FastAPI()
│   │   ├── models.py     # Pydantic/SQLAlchemy
│   │   ├── schemas.py    # Pydantic схемы
│   │   ├── database.py   # DB сессии
│   │   └── api/          # Роуты
│   │       ├── __init__.py
│   │       ├── auth.py
│   │       ├── posts.py
│   │       └── users.py
│   ├── tests/            # Pytest
│   │   └── test_posts.py
│   ├── migrations/       # Alembic (если SQLAlchemy)
│   ├── static/           # Загружаемые файлы (avatars, не /media)
│   ├── database.db       # Только для dev, в prod — Docker volume
│   ├── requirements.txt
│   ├── .env              # DB_URL, SECRET_KEY
│   └── Dockerfile
├── frontend/             # Vue.js SPA (ваш dist)
│   ├── dist/             # Сборка (index.html, js/, css/)
│   ├── public/           # Статические (favicon)
│   ├── src/              # Исходники (компоненты)
│   ├── package.json
│   └── Dockerfile        # nginx -g 'daemon off;' для статики
├── docker-compose.yml    # backend + frontend + postgres/redis
├── nginx.conf            # Reverse proxy (API → backend, SPA → frontend)
├── .env
├── .gitignore
└── README.md

/backend/
├── static/
│   └── media/
│       ├── tweets/     # /tweets/{tweet_id}/{filename}
│       │   └── 123/abc.jpg
│       └── avatars/    # /avatars/{user_id}/{filename}
│           └── 456/photo.jpg